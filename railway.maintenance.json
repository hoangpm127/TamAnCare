{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "echo Maintenance image ready"
  },
  "deploy": {
    "startCommand": "npm run job:maintenance",
    "cronSchedule": "*/5 * * * *",
    "restartPolicyType": "NEVER"
  }
}
